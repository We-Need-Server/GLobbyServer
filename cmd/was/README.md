# /cmd/was

This directory contains the entry point (`main.go`) for the Web Application Server (WAS).

This application is responsible for:
-   Running the Gin web server.
-   Serving server-side rendered HTML pages to users.
-   Handling user registration and login via web forms.
