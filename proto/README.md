# /proto

This directory contains the Protocol Buffer (`.proto`) definition files for our gRPC services.

The Go code generated from these files is used by both the gRPC server for its implementation and by gRPC clients (other microservices) to communicate with this service.
